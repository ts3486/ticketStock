input EventInput {
  name: string
  image: string
  desc: string
  date: DateTime
}

input TicketInput {
  name: string
  image: string
  date: DateTime
  price: number
}

mutation AddEvent($event: EventInput!, $ticket: TicketInput!) {
  addEvent(event: $event, ticket: $ticket)
}
